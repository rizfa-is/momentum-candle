"""MT5 MCP MVP — momentum-candle.

Public modules:
    client    — terminal initialization and connection guard
    constants — string ↔ MetaTrader5 numeric constants
    account   — account info
    market    — symbol price + candles
    trade     — market orders, modify, close, close-all
    server    — FastMCP server with 8 tools
"""

__version__ = "0.1.0"
